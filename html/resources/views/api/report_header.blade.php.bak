@if(app()->getLocale() == 'kh')
     <div class="report_header">
            <div class="report_logo">
                <img src="{{ asset('images/T&Go.png', true) }}" style="width: 104px;">
            </div>
            <div class="company_address" style="font-size: 10px;">
                <p style="font-weight: bold;">ទីតាំងការិយាល័យ </p>
                <p>{{EN_HO_ADDRESS}}</p>
                <p>{{EN_HO_TEL}}</p>
                @if(!empty($co_phone))
                    <p>CO Tel : {{$co_phone}} </p>
                @endif
            </div>
        </div>
@else
    <div class="report_header">
        <div class="report_logo">
            <img src="{{ asset('images/T&Go.png', true) }}" style="width: 104px;">
        </div>

        <div class="company_address" style="font-size: 10px;">
            <p style="font-weight: bold;">Office Address</p>
            <p>{{EN_HO_ADDRESS}}</p>
            <p>{{EN_HO_TEL}}</p>
            @if(!empty($co_phone))
                <p>CO Tel : {{$co_phone}} </p>
            @endif
        </div>
    </div>

@endif